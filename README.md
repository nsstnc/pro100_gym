## Запуск проекта

```bash
make build up
```

#### Посмотреть другие команды make

```bash
make help
```

## PRO-30: Авторизация через Telegram

### Что сделали
Переделали Telegram интеграцию под локальную работу. Убрали редиректы через сайт, теперь бот сам спрашивает логин/пароль.

### Как работает авторизация

#### Способ 1: Через сайт в Telegram (работает локально!)
1. Пользователь авторизуется на сайте
2. Нажимает "Telegram" в навбаре → переходит на `/connect`
3. Нажимает "Открыть Telegram бота"
4. Frontend получает JWT токен: `GET /auth/telegram-link`
5. Открывается `https://t.me/bot?start=JWT_TOKEN`
6. Бот получает `/start JWT_TOKEN`
7. Бот отправляет токен в backend: `POST /auth/link-telegram`
8. Backend декодирует токен → связывает `telegram_id` с пользователем

#### Способ 2: Прямая авторизация в боте (работает локально)
1. Пользователь пишет `/start` боту
2. Бот проверяет `telegram_id` в базе: `GET /users/by-telegram/{telegram_id}`
3. Если аккаунт уже привязан → приветствие
4. Если нет → бот начинает онбординг:
   - Предлагает кнопку "🚀 Авторизоваться"
   - Спрашивает username
   - Спрашивает password
   - Отправляет данные: `POST /auth/bot-login`
5. Backend проверяет credentials → обновляет `telegram_id` в таблице users

#### API endpoints
- `GET /auth/telegram-link` - получить JWT токен и ссылку на бота
- `POST /auth/link-telegram` - связать аккаунты через токен
- `GET /users/by-telegram/{telegram_id}` - проверить привязан ли аккаунт
- `POST /auth/bot-login` - авторизовать через логин/пароль в боте

#### Переменные окружения для бота
```
BOT_TOKEN=токен_бота
API_BASE_URL=http://localhost:8000
```

#### Состояния в боте
```python
user_states = {}  # user_id -> {'state': 'waiting_login' | 'waiting_password', 'login': str}
```

### Запуск для тестирования
1. Запустить backend
2. Запустить бота: `cd bot && python main.py`
3. Написать боту `/start`
4. Ввести логин/пароль от сайта

## Как работаем с репой

### 0. Получили задачу в YouGile и берем ее slug
<img width="561" height="328" alt="image" src="https://github.com/user-attachments/assets/30fd75d6-0c2a-4488-a8f8-dc389f6dee49" />

### 1. Прежде чем делать задачу, переключаемся на main и обновляем состояние проекта

```bash
git checkout main
```

```bash
git pull origin main
```

### 2. Cоздаем git-ветку с названием в виде слага

```bash
git checkout -b PRO-8
```

### 3. Делаем задачу, коммитим в созданную ветку. Название коммитов начинаем со слага

```bash
git commit -m 'PRO-8: добавил фичу'
```

### 4. Закончили делать задачу и убедились, что все работает - пушим ветку

```bash
git push --set-upstream origin PRO-8
```

### 5. Делаем ПР из этой ветки в main. В YouGile переводим задачу в "Тестируются"
### 6. Просим коллег поревьюить и потестить выполненную задачу
