Как запускать и тестировать Telegram-бота Pro100 Gym
Требования

Docker и Docker Compose (рекомендуется)
Или Python 3.11+ с установленными зависимостями

Вариант 1: Запуск через Docker Compose (рекомендуется)
Это самый простой и стабильный способ — запускает бот, бэкенд и БД одновременно.

Склонируй репозиторий: git clone https://github.com/nsstnc/pro100_gym.git
cd pro100_gym
Создай файл .env в папке bot со следующими переменными:
BOT_TOKEN=8430286281:AAHXGSoSToI4eVvfhoKkwVly78UfH2TJBmA
USE_WEBHOOK=false
WEBHOOK_URL=https://your-domain.com/webhook
WEBAPP_HOST=0.0.0.0
WEBAPP_PORT=8080
API_URL=http://backend:8000/
API_USERNAME=Mgmyrin
API_PASSWORD=telegram

Запусти всё одной командам
docker-compose up --build
Бот запустится в режиме polling и будет доступен в Telegram.

Вариант 2: Запуск бота отдельно (без бэкенда и БД)
Подходит для локального тестирования логики бота (с fake-данными).

Установи зависимости:Bashpip install -r requirements.txt
В файле api.py установи:PythonUSE_FAKE_BACKEND = True
Создай .env с токеном:envBOT_TOKEN=твой_токен_от_BotFather
Запусти бота:Bashpython main.py

Тестирование бота

Найди бота в Telegram по username @pro100gym_bot
Нажми /start.
Пройди онбординг (🧩 Онбординг) — введи данные.
После генерации плана нажми 💪 Тренировка:
Выбери день → начнётся сессия.
Нажимай "Выполнить" или "Пропустить" для каждого подхода.
После завершения тренировки бот предложит выбрать день недели для следующей.

Проверь напоминания и мотивацию (работают в фоне).

Остановить Docker: Ctrl+C, затем docker compose down

